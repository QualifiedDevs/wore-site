import { styled } from "@mui/material/styles";
import { Button } from "@mui/material";

const FillButton = styled((props) => {
  return <Button {...props} />;
})`
  :hover {
      
  }
`;

export default FillButton;
